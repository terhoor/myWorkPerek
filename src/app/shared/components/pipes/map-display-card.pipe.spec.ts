import { MapDisplayCardPipe } from './map-display-card.pipe';

describe('MapDisplayCardPipe', () => {
  it('create an instance', () => {
    const pipe = new MapDisplayCardPipe();
    expect(pipe).toBeTruthy();
  });
});
